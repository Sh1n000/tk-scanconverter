
# Required minimum versions for this item to run
# requires_shotgun_version:
# requires_core_version: "v0.14.28"
# requires_engine_version:

# the frameworks required to run this app
# frameworks:

name: tk-scanconverter
display_name: "Scan Converter"
description: "A toolkit app to manage Scan Data I/O in Shotgun."
engine: tk-desktop
version: 0.1.0

configuration:

  # review_file_template:
  #   type: template
  #   description: "Template for the exported review file."
  #   fields: version, *

# supported_engines:  tk-nuke, tk-maya